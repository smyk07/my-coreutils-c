# Replicating `coreutils`

These are my attempts at replicating some linux `coreutils` in C.

## `coreutils` implemented until now

- `cat`
- `echo`

## Usage

### Compile

```
make
```

Or

```
make {util}
```

### Run

An Example:

```
./cat cat.c
```
